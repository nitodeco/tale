import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontSize: 24,
    fontWeight: 'bold',
    color: 'white',
  },
  author: {
    fontSize: 16,
    color: 'white',
  },
}); 

export default styles;